# Teórica 03

**¿Cómo se calcula la complejidad de dos bucles anidados? ¿Y de uno que hace `i *= 2`?**

Dos bucles **anidados** de n vueltas cada uno: se **multiplican** → O(n²). Un bucle que multiplica el índice por 2 (`i *= 2`) da ~log₂n vueltas → O(log n).
