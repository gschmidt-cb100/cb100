# Teórica 10

**¿Qué diferencia hay entre `HashSet`, `LinkedHashSet` y `TreeSet`?**

`HashSet`: sin orden garantizado, operaciones O(1) promedio. `LinkedHashSet`: mantiene el **orden de inserción**. `TreeSet`: mantiene los elementos **ordenados** (orden natural o Comparator), con operaciones O(log n).
