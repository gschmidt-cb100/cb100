# Teórica 05

**Explicá la diferencia entre `==` y `equals` con un ejemplo donde den distinto.**

`==` compara **identidad** (¿mismo objeto?); `equals` compara **contenido** (¿mismo valor?). Ejemplo: `new String("java") == new String("java")` da `false`, pero `.equals(...)` da `true`.
