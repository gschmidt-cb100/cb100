# Teórica 03

**Explicá flotar y hundir. ¿Cuál es el costo de cada uno y por qué?**

**Flotar** (*sift-up*) repara el invariante hacia arriba: un elemento recién insertado en la última posición se compara con su padre y se intercambia mientras sea menor; se detiene al encontrar un padre menor o igual (o llegar a la raíz). Se usa al **encolar**.

**Hundir** (*sift-down*) repara hacia abajo: un elemento se compara con **el menor de sus dos hijos** y se intercambia si ese hijo es menor; se repite hasta que sea menor que ambos (o llegue a hoja). Se usa al **desencolar** (cuando la última hoja sube a la raíz) y en heapify.

Ambos cuestan $O(\log n)$: cada intercambio mueve el elemento exactamente un nivel, y un árbol completo de $n$ nodos tiene altura $\lfloor \log_2 n \rfloor$ — ése es el máximo de niveles que se pueden subir o bajar.
