# Teórica 08

**Compará el heap contra lista ordenada, lista desordenada y AVL para el contrato de cola de prioridad.**

- **Lista ordenada**: ver mínimo y desencolar $O(1)$, pero **encolar** debe insertar en orden: $O(n)$. Buena sólo si casi no se encola.
- **Lista desordenada**: encolar $O(1)$, pero ver/desencolar el mínimo **busca**: $O(n)$. Buena sólo si casi no se desencola.
- **AVL**: todo $O(\log n)$… pero mantiene **orden total** (mucho más de lo que el contrato pide), con nodos, referencias y rotaciones: más memoria y peores constantes.
- **Heap**: ver mínimo $O(1)$, encolar y desencolar $O(\log n)$, construcción $O(n)$ con heapify, y vive en un **arreglo compacto**. Mantiene sólo el invariante débil necesario para conocer al mínimo — por eso gana el combinado y es la implementación estándar.
