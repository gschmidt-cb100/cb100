# Teórica 05

**¿Por qué `7 / 2` da `3`? ¿Cómo obtenés `3.5`?**

Porque la división entre dos **enteros** es entera: descarta el decimal. Para obtener `3.5`, al menos un operando debe ser `double`: `7.0 / 2` o `(double) 7 / 2`.
