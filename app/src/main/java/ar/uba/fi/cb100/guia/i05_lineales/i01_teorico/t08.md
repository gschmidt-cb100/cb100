# Teórica 08

**Asociá cada estructura vista con su equivalente en la API de Java.**

VectorDinamico → `ArrayList`; listas enlazadas → `LinkedList`; ConjuntoLista → `HashSet` (sin orden), `LinkedHashSet` (orden de inserción) o `TreeSet` (ordenado). Las interfaces son `List` y `Set`.
