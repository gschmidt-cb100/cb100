# Teórica 03

**¿Cuál es la diferencia entre una clase y un objeto?**

Una **clase** es el molde: define el estado (campos) y el comportamiento (métodos). Un **objeto** es una instancia concreta de esa clase, creada con `new`, con sus propios valores. De una clase se crean muchos objetos.
