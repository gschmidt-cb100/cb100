# Teórica 06

**¿Qué genera un `record`? ¿Cuándo conviene usarlo?**

Genera automáticamente el constructor, los accesores de cada campo y los métodos `equals`, `hashCode` y `toString`. Conviene para **datos inmutables** que sólo agrupan valores (un punto, una temperatura), ahorrando código repetitivo.
